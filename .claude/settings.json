{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && git pull 2>&1 | tail -5",
            "statusMessage": "Checking for updates..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "printf '# Last Modified\\n\\n**Time:** %s  \\n**Machine:** %s\\n' \"$(date '+%Y-%m-%d %H:%M:%S')\" \"$(hostname)\" > \"$CLAUDE_PROJECT_DIR/LAST_MODIFIED.md\" && cd \"$CLAUDE_PROJECT_DIR\" && git add -A && git commit -m \"auto snapshot $(date +%F_%T)\" && git push 2>&1 | tail -3"
          }
        ]
      }
    ]
  }
}
